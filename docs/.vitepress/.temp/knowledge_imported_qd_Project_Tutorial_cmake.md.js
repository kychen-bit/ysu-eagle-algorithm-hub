import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"❗学习要求：","description":"","frontmatter":{"source_type":"repost","source_project":"QD Algorithm Library","source_url":"https://github.com/NOMANE-0/QD_Algorithm_Library/blob/main/docs/Project_Tutorial/cmake.md","license":"Apache-2.0"},"headers":[],"relativePath":"knowledge/imported/qd/Project_Tutorial/cmake.md","filePath":"knowledge/imported/qd/Project_Tutorial/cmake.md","lastUpdated":null}');
const _sfc_main = { name: "knowledge/imported/qd/Project_Tutorial/cmake.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><p>cmake 是⼀种常⽤、⽅便的，⽤于组织 Linux 下 C++ 程序的⼯具。有许多库，例如 OpenCV、g2o、</p><p>Ceres 等，都⽤ cmake 组织它们的⼯程。所以，不管是使⽤别⼈的库，还是编写⾃⼰的库，都需要掌握</p><p>⼀些 cmake 的基本知识。 本教程基于**<a href="http://file.ncnynl.com/ros/CMake%20Practice.pdf" target="_blank" rel="noreferrer">Cmake Practice.pdf</a>**（cmake 实践，由⼀位北⼤同学撰写） 为了阅读方便，本文档整理摘抄并修改自互联网上的PDF版本，仅供学习参考使用。</p><h1 id="❗学习要求" tabindex="-1">❗<strong>学习要求：</strong> <a class="header-anchor" href="#❗学习要求" aria-label="Permalink to &quot;❗**学习要求：**&quot;">​</a></h1><p>请阅读此⽂的第 1 ⾄ 6 章，并完成以下⼯作：</p><p>书写⼀个由 cmake 组织的 C++ ⼯程，要求如下：</p><ol><li><p>include/hello.h 和 src/hello.c 构成了 libhello.so 库。hello.c 中提供⼀个函数 sayHello()，调⽤此函数时往屏幕输出⼀⾏“Hello World”。我们已经为你准备了 hello.h 和 hello.c 这两个代码在下方。</p></li><li><p>⽂件 useHello.c 中含有⼀个 main 函数，它可以编译成⼀个可执⾏⽂件，名为“sayhello”</p></li><li><p>默认⽤ Release 模式编译这个⼯程。</p></li><li><p>如果⽤户使⽤ sudo make install，那么将 hello.h 放⾄/usr/local/include/下，将 libhello.so 放⾄/usr/local/lib/下。</p></li></ol><p>请按照上述要求组织源代码⽂件，并书写 CMakeLists.txt。</p><div class="language-C++ vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">C++</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">#pragma</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> once</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">void</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> sayHello</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">();</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><div class="language-C++ vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">C++</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">#include</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;hello.h&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">#include</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &lt;iostream&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">void</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> sayHello</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() {</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">std</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">::cout</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;Hello World&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">std</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">::endl;}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><div class="language-C++ vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">C++</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">#include</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;hello.h&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">int</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> main</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">( </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">int</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}"> argc</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">char**</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}"> argv</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  sayHello</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">();</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><h1 id="一、初识cmake" tabindex="-1">一、初识Cmake <a class="header-anchor" href="#一、初识cmake" aria-label="Permalink to &quot;一、初识Cmake&quot;">​</a></h1><p>略</p><h1 id="二、安装cmake" tabindex="-1">二、安装Cmake <a class="header-anchor" href="#二、安装cmake" aria-label="Permalink to &quot;二、安装Cmake&quot;">​</a></h1><p>略</p><h1 id="三、初试cmake-cmake-的helloworld" tabindex="-1">三、初试cmake - cmake 的helloworld <a class="header-anchor" href="#三、初试cmake-cmake-的helloworld" aria-label="Permalink to &quot;三、初试cmake - cmake 的helloworld&quot;">​</a></h1><p>本节选择了一个最简单的例子 Helloworld 来演练一下 cmake 的完整构建过程，本节并不</p><p>会深入的探讨 cmake，仅仅展示一个简单的例子，并加以粗略的解释。</p><h2 id="_1-准备工作" tabindex="-1">1.准备工作 <a class="header-anchor" href="#_1-准备工作" aria-label="Permalink to &quot;1.准备工作&quot;">​</a></h2><p>首先，在 <code>/backup</code> 目录建立一个 <code>cmake</code> 目录，用来放置我们学习过程中的所有练习。</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">mkdir</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -p</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /backup/cmake</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>以后我们所有的 cmake 练习都会放在/backup/cmake 的子目录下(你也可以自行安排目录，这个并不是限制，仅仅是为了叙述的方便)。</p><p>然后在 cmake 建立第一个练习目录 <code>t1</code>：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">cd</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /backup/cmake</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">mkdir</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> t1</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">cd</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> t1</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>在 t1 目录建立 <code>main.c</code> 和 <code>CMakeLists.txt</code>(注意文件名大小写)：</p><p><code>main.c</code> 文件内容：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">//</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> main.c</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">#include &lt;stdio.h&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">int</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> main</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">()</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    printf</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">&quot;Hello World from t1 Main!\\n&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    return</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p><code>CmakeLists.txt</code> 文件内容：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">PROJECT(HELLO</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">SET(SRC_LIST</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> main.c</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">MESSAGE(STATUS</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;This is BINARY dir &quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> \${HELLO_BINARY_DIR})</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">MESSAGE(STATUS</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;This is SOURCE dir &quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> \${HELLO_SOURCE_DIR})</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ADD_EXECUTABLE(hello</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> \${SRC_LIST})</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><h2 id="_2-开始构建" tabindex="-1">2.开始构建 <a class="header-anchor" href="#_2-开始构建" aria-label="Permalink to &quot;2.开始构建&quot;">​</a></h2><p>所有的文件创建完成后，<code>t1</code> 目录中应该存在 <code>main.c</code> 和 <code>CMakeLists.txt</code> 两个文件。接下来我们来构建这个工程，在这个目录运行：</p><p><code>cmake .</code> (注意命令后面的点号，代表本目录)。·</p><p>输出大概是这个样子：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">--</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> Check</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> for</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> working</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> C</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> compiler:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /usr/bin/gcc</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">--</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> Check</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> for</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> working</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> C</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> compiler:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /usr/bin/gcc</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> works</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">--</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> Check</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> size</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> of</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> void</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">*</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">--</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> Check</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> size</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> of</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> void</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">*</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> -</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> done</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">--</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> Check</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> for</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> working</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> CXX</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> compiler:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /usr/bin/c++</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">--</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> Check</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> for</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> working</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> CXX</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> compiler:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /usr/bin/c++</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> works</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">--</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> This</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> is</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> BINARY</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dir</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /backup/cmake/t1</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">--</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> This</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> is</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> SOURCE</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dir</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /backup/cmake/t1</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">--</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> Configuring</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> done</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">--</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> Generating</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> done</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">--</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> Build</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> files</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> have</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> been</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> written</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> to:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /backup/cmake/t1</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><p>再让我们看一下目录中的内容，你会发现，系统自动生成了：</p><p><code>CMakeFiles</code>，<code>CMakeCache.txt</code>，<code>cmake_install.cmake</code> 等文件，并且生成了 <code>Makefile</code>。</p><p>然后进行工程的<strong>实际构建</strong>，在这个目录输入 <code>make</code> 命令，大概会得到如下的彩色输出：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">Scanning</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dependencies</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> of</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> target</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> hello</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[100%] Building C object CMakeFiles/hello.dir/main.o</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">Linking</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> C</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> executable</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> hello</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[100%] Built target hello</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>如果你需要看到 <code>make</code> 构建的详细过程，可以使用 <code>make VERBOSE=1</code> 或者 <code>VERBOSE=1 make</code> 命令来进行构建。</p><p>这时候，我们需要的目标文件 hello 已经构建完成，位于当前目录，尝试运行一下：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">./hello</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>得到输出：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">Hello</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> World</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> t1</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> Main!</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>恭喜您，到这里为止您已经完全掌握了 cmake 的使用方法。</p><h2 id="_3-简单解释" tabindex="-1">3.简单解释 <a class="header-anchor" href="#_3-简单解释" aria-label="Permalink to &quot;3.简单解释&quot;">​</a></h2><p>我们来重新看一下 <code>CMakeLists.txt</code>，这个文件是 cmake 的构建定义文件，文件名是大小写相关的，如果工程存在多个目录，需要确保每个要管理的目录都存在一个 <code>CMakeLists.txt</code>。(关于多目录构建，后面我们会提到，这里不作过多解释)。</p><p>上面例子中的 CMakeLists.txt 文件内容如下：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">PROJECT</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (HELLO)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">SET(SRC_LIST</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> main.c</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">MESSAGE(STATUS</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;This is BINARY dir &quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> \${HELLO_BINARY_DIR})</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">MESSAGE(STATUS</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;This is SOURCE dir &quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">\${HELLO_SOURCE_DIR})</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ADD_EXECUTABLE(hello</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> \${SRC_LIST})</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><h3 id="project-指令的语法" tabindex="-1"><em>PROJECT 指令的语法</em> <a class="header-anchor" href="#project-指令的语法" aria-label="Permalink to &quot;*PROJECT 指令的语法*&quot;">​</a></h3><p><code>PROJECT(projectname [CXX] [C] [Java])</code></p><p>你可以用这个指令定义工程名称，并可指定工程支持的语言，支持的语言列表是可以忽略的，默认情况表示支持所有语言。这个指令隐式的定义了两个 cmake 变量：</p><p><code>&lt;projectname&gt;_BINARY_DIR</code> 以及 <code>&lt;projectname&gt;_SOURCE_DIR</code>，这里就是 <code>HELLO_BINARY_DIR</code> 和 <code>HELLO_SOURCE_DIR</code> (所以 <code>CMakeLists.txt</code> 中两个 <code>MESSAGE</code> 指令可以直接使用了这两个变量)，因为采用的是内部编译，两个变量目前指的都是工程所在路径<code>/backup/cmake/t1</code>，后面我们会讲到外部编译，两者所指代的内容会有所不同。</p><p>同时 cmake 系统也帮助我们预定义了 <code>PROJECT_BINARY_DIR</code> 和 <code>PROJECT_SOURCE_DIR</code> 变量，他们的值分别跟 <code>HELLO_BINARY_DIR</code> 与 <code>HELLO_SOURCE_DIR</code> 一致。</p><p>为了统一起见，建议以后直接使用<code>PROJECT_BINARY_DIR</code>和<code>PROJECT_SOURCE_DIR</code>，即使修改了工程名称，也不会影响这两个变量。如果使用了<code>&lt;projectname_SOURCE_DIR&gt;</code>，修改工程名称后，需要同时修改这些变量。</p><h3 id="set指令的语法" tabindex="-1"><em>SET指令的语法</em> <a class="header-anchor" href="#set指令的语法" aria-label="Permalink to &quot;*SET指令的语法*&quot;">​</a></h3><p><code>SET(VAR [VALUE] [CACHE TYPE DOCSTRING [FORCE]])</code></p><p>现阶段，你只需要了解 SET 指令可以用来显式的定义变量即可。比如我们用到的是<code>SET(SRC_LIST main.c)</code>，如果有多个源文件，也可以定义成：<code>SET(SRC_LIST main.c t1.c t2.c)</code>。</p><h3 id="message指令的语法" tabindex="-1"><em>MESSAGE指令的语法</em> <a class="header-anchor" href="#message指令的语法" aria-label="Permalink to &quot;*MESSAGE指令的语法*&quot;">​</a></h3><p><code>MESSAGE([SEND_ERROR | STATUS | FATAL_ERROR] &quot;message to display&quot; ...)</code></p><p>这个指令用于向终端输出用户定义的信息，包含了三种类型：</p><ol><li><p><code>SEND_ERROR</code>，产生错误，生成过程被跳过</p></li><li><p><code>STATUS</code>，输出前缀为<code>--</code>的信息</p></li><li><p><code>FATAL_ERROR</code>，立即终止所有cmake过程</p></li></ol><p>我们在这里使用的是 <code>STATUS</code> 信息输出，演示了由 <code>PROJECT</code> 指令定义的两个隐式变量 <code>HELLO_BINARY_DIR</code> 和 <code>HELLO_SOURCE_DIR</code>。</p><h3 id="add-executable指令的语法" tabindex="-1"><em>ADD_EXECUTABLE指令的语法</em> <a class="header-anchor" href="#add-executable指令的语法" aria-label="Permalink to &quot;*ADD_EXECUTABLE指令的语法*&quot;">​</a></h3><p><strong><code>ADD_EXECUTABLE(hello \${SRC_LIST})</code></strong></p><p>定义了这个工程会生成一个文件名为 hello 的可执行文件，相关的源文件是 SRC_LIST 中定义的源文件列表， 本例中你也可以直接写成<code>ADD_EXECUTABLE(hello main.c)</code>。</p><p>在本例我们使用了<code>\${}</code>来引用变量，这是 cmake 的变量应用方式，但是，有一些例外，比如在 <code>IF 控制语句</code>，变量是直接使用变量名引用，而不需要<code>\${}</code>。如果使用了<code>\${}</code>去应用变量，其实 IF 会去判断名为<code>\${}</code>所代表的值的变量，那当然是不存在的了。</p><p>将本例改写成一个最简化的<code>CMakeLists.txt</code>：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">PROJECT(HELLO</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ADD_EXECUTABLE(hello</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> main.c</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><h2 id="_4-基本语法规则" tabindex="-1">4.基本语法规则 <a class="header-anchor" href="#_4-基本语法规则" aria-label="Permalink to &quot;4.基本语法规则&quot;">​</a></h2><p>前面提到过，cmake 其实仍然要使用”cmake 语言和语法”去构建，上面的内容就是所谓的 ”cmake 语言和语法”，最简单的语法规则是：</p><ol><li><p>变量使用<code>\${}</code>方式取值，但是在 IF 控制语句中是直接使用变量名。</p></li><li><p>指令(参数1 参数2...)</p><p>参数使用括弧括起，参数之间使用空格或分号分开。</p><p>以上面的 <code>ADD_EXECUTABLE</code> 指令为例，如果存在另外一个 func.c 源文件，就要写成：</p><p><code>ADD_EXECUTABLE(hello main.c func.c</code>)或者</p><p><code>ADD_EXECUTABLE(hello main.c;func.c)</code></p></li><li><p>指令是大小写无关的，参数和变量是大小写相关的。但，推荐你全部使用大写指令。</p><p>上面的 MESSAGE 指令已经用到了这条规则：</p><p><code>MESSAGE(STATUS “This is BINARY dir” \${HELLO_BINARY_DIR})</code></p><p>也可以写成：</p><p><code>MESSAGE(STATUS “This is BINARY dir \${HELLO_BINARY_DIR}”)</code></p></li></ol><p><strong>这里需要特别解释的是作为工程名 HELLO 和生成的可执行文件 hello 是没有任何关系的。hello 定义的可执行文件的文件名，你完全可以写成：<code>ADD_EXTCUTABLE(t1 main.c)</code>，编译后会生成一个 t1 可执行文件。</strong></p><h2 id="_5-关于语法的疑惑" tabindex="-1">5.关于语法的疑惑 <a class="header-anchor" href="#_5-关于语法的疑惑" aria-label="Permalink to &quot;5.关于语法的疑惑&quot;">​</a></h2><p>cmake 的语法还是比较灵活而且考虑到各种情况，比如 <code>SET(SRC_LIST main.c)</code> 也可以写成 <code>SET(SRC_LIST &quot;main.c&quot;)</code> 是没有区别的，但是假设一个源文件的文件名是 <code>fu nc.c</code>(文件名中间包含了空格)。 这时候就必须使用双引号，如果写成了SET(SRC_LIST fu nc.c)，就会出现错误，提示你找不到 <code>fu 文件</code>和 <code>nc.c 文件</code>。这种情况，就必须写成：<code>SET(SRC_LIST &quot;fu nc.c&quot;)</code>。</p><p>此外，你可以可以忽略掉 source 列表中的源文件后缀，比如可以写成 <code>ADD_EXECUTABLE(t1 main)</code>，cmake 会自动的在本目录查找 <code>main.c</code> 或者 <code>main.cpp</code> 等，当然，最好不要偷这个懒，以免这个目录确实存在一个 <code>main.c</code> 一个 <code>main</code>。</p><p>同时参数也可以使用分号来进行分割。</p><p>下面的例子也是合法的：</p><p><code>ADD_EXECUTABLE(t1 main.c t1.c)</code> 可以写成 <code>ADD_EXECUTABLE(t1 main.c;t1.c)</code>。</p><p>我们只需要在编写 <code>CMakeLists.txt</code> 时注意形成统一的风格即可。</p><h2 id="_6-清理工程" tabindex="-1">6.清理工程 <a class="header-anchor" href="#_6-清理工程" aria-label="Permalink to &quot;6.清理工程&quot;">​</a></h2><p>跟经典的 autotools 系列工具一样，运行：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> clean</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>即可对构建结果进行清理。</p><h2 id="_7-问题-问题" tabindex="-1">7.问题？问题！ <a class="header-anchor" href="#_7-问题-问题" aria-label="Permalink to &quot;7.问题？问题！&quot;">​</a></h2><p>“我尝试运行了 <code>make distclean</code>，这个指令一般用来清理构建过程中产生的中间文件的，如果要发布代码，必然要清理掉所有的中间文件，但是为什么在 cmake 工程中这个命令是无效的?”</p><p>是的，cmake 并不支持 <code>make distclean</code>，关于这一点，官方是有明确解释的：</p><blockquote><p>因为 <code>CMakeLists.txt</code> 可以执行脚本并通过脚本生成一些临时文件，但是却没有办法来跟踪这些临时文件到底是哪些。因此，没有办法提供一个可靠的 make distclean 方案。</p></blockquote><blockquote><p>Some build trees created with GNU autotools have a &quot;make distclean&quot; target that cleans the build and also removes Makefiles and other parts of the generated build system. CMake does not generate a &quot;make distclean&quot; target because <code>CMakeLists.txt</code> files can run scripts and arbitrary commands; CMake has no way of tracking exactly which files are generated as part of running CMake. Providing a distclean target would give users the false impression that it would work as expected. (CMake does generate a &quot;make clean&quot; target to remove files generated by the compiler and linker.)</p></blockquote><blockquote><p>A &quot;make distclean&quot; target is only necessary if the user performs an in-source build. CMake supports in-source builds, but we strongly encourage users to adopt the notion of an out-of-source build. Using a build tree that is separate from the source tree will prevent CMake from generating any files in the source tree. Because CMake does not change the source tree, there is no need for a distclean target. One can start a fresh build by deleting the build tree or creating a separate build tree.</p></blockquote><p>同时，还有另外一个非常重要的提示，就是：我们刚才进行的是内部构建(in-source build)，而 cmake 强烈推荐的是外部构建(out-of-source build)。</p><h2 id="_8-内部构建与外部构建" tabindex="-1">8.内部构建与外部构建 <a class="header-anchor" href="#_8-内部构建与外部构建" aria-label="Permalink to &quot;8.内部构建与外部构建&quot;">​</a></h2><p>在使用构建系统（如 CMake）时，<strong>内部构建</strong>会在源代码目录生成大量中间文件，难以管理和清理，因此推荐使用<strong>外部构建（Out-of-Source Build）</strong>。</p><h3 id="🚀-外部构建简介" tabindex="-1">🚀 外部构建简介： <a class="header-anchor" href="#🚀-外部构建简介" aria-label="Permalink to &quot;🚀 外部构建简介：&quot;">​</a></h3><p>以编译 wxGTK 静态库和动态库为例：</p><ol><li><p>解压源码后创建 <code>static</code> 和 <code>shared</code> 两个子目录。</p></li><li><p>进入 <code>static</code>，执行：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">.</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">./configure</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --enable-static</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>会生成静态库。</p></li><li><p>进入 <code>shared</code>，执行：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">.</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">./configure</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --enable-shared</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>会生成动态库。</p></li></ol><p>这种方式将构建文件与源码隔离，便于管理。</p><hr><h3 id="🛠-cmake-外部构建流程" tabindex="-1">🛠 CMake 外部构建流程： <a class="header-anchor" href="#🛠-cmake-外部构建流程" aria-label="Permalink to &quot;🛠 CMake 外部构建流程：&quot;">​</a></h3><ol><li><p>清理 <code>t1</code> 目录中除 <code>main.c</code> 和 <code>CMakeLists.txt</code> 外的文件（特别是 <code>CMakeCache.txt</code>）。</p></li><li><p>在 <code>t1</code> 中或任意位置新建 <code>build</code> 目录。</p></li><li><p>进入 <code>build</code>，执行：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">cmake</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ..</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>生成 Makefile 和中间文件。</p></li><li><p>运行：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>编译输出目标程序 <code>hello</code>，且所有中间文件仅存在于 <code>build</code> 目录中。</p></li></ol><blockquote><p>✅ <strong>优点</strong>：原始工程目录保持干净，构建过程可控、易清理。</p></blockquote><hr><h3 id="📌-小贴士" tabindex="-1">📌 小贴士： <a class="header-anchor" href="#📌-小贴士" aria-label="Permalink to &quot;📌 小贴士：&quot;">​</a></h3><ul><li><p><code>HELLO_SOURCE_DIR</code>：源代码路径，如 <code>/backup/cmake/t1</code></p></li><li><p><code>HELLO_BINARY_DIR</code>：构建路径，如 <code>/backup/cmake/t1/build</code></p></li></ul><h2 id="_9-小结" tabindex="-1">9.小结 <a class="header-anchor" href="#_9-小结" aria-label="Permalink to &quot;9.小结&quot;">​</a></h2><p>本小节介绍了使用 CMake 构建 Hello World 程序的完整流程，涵盖了四个基本指令：<code>PROJECT</code>、<code>SET</code>、<code>MESSAGE</code>、<code>ADD_EXECUTABLE</code>，以及变量调用的方法。同时提到了两个隐式变量：<code>&lt;projectname&gt;_SOURCE_DIR</code> 和 <code>&lt;projectname&gt;_BINARY_DIR</code>。</p><p>尽管对于简单项目，直接用 <code>gcc main.c</code> 或手写 Makefile 更快捷，但当面对如 <strong>kdelibs（压缩包高达 50M）</strong> 这类大型工程时，使用 CMake 无疑更加高效和可维护。</p><p>下一节，我们的任务是让 Hello World 看起来更像一个工程。</p><h1 id="四、更好一点的hello-world" tabindex="-1">四、更好一点的Hello World <a class="header-anchor" href="#四、更好一点的hello-world" aria-label="Permalink to &quot;四、更好一点的Hello World&quot;">​</a></h1><p>从本小节开始，后面所有的构建我们都将采用 out-of-source 外部构建，约定的构建目录是工程目录下的 build 自录。</p><p>本小节的任务是让前面的 Hello World 更像一个工程，我们需要做的是：</p><ol><li><p>为工程添加一个<code>子目录 src</code>，用来放置工程源代码</p></li><li><p>添加一个<code>子目录 doc</code>，用来放置这个工程的文档 <code>hello.txt</code></p></li><li><p>在工程目录添加文本文件 <code>COPYRIGHT</code>，<code>README</code></p></li><li><p>在工程目录添加一个 <code>runhello.sh</code> 脚本，用来调用 hello 二进制</p></li><li><p>将构建后的目标文件放入构建目录的 <code>bin 子目录</code></p></li><li><p>最终安装这些文件：将 <code>hello</code> 二进制与 <code>runhello.sh</code> 安装至<code>/usr/bin</code>，将<code>doc 目录</code>的内容以及 <code>COPYRIGHT/README</code> 安装到<code>/usr/share/doc/cmake/t2</code></p></li></ol><h2 id="_1-准备工作-1" tabindex="-1">1.准备工作 <a class="header-anchor" href="#_1-准备工作-1" aria-label="Permalink to &quot;1.准备工作&quot;">​</a></h2><p>在<code>/backup/cmake/</code>目录下建立 <code>t2 目录</code>。</p><p>将 t1 工程的 <code>main.c</code> 和 <code>CMakeLists.txt</code> 拷贝到 t2 目录中。</p><h2 id="_2-添加子目录src" tabindex="-1">2.添加子目录src <a class="header-anchor" href="#_2-添加子目录src" aria-label="Permalink to &quot;2.添加子目录src&quot;">​</a></h2><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">mkdir</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> src</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">mv</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> main.c</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> src</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>现在的工程看起来是这个样子：</p><p>一个子目录 src，一个 CMakeLists.txt。</p><p>上一节我们提到，需要为任何子目录建立一个 CMakeLists.txt，进入子目录 src，编写 CMakeLists.txt 如下：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ADD_EXECUTABLE(hello</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> main.c</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>将 t2 工程的 CMakeLists.txt 修改为：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">PROJECT(HELLO</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> C</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ADD_SUBDIRECTORY(src</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> bin</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>然后建立 build 目录，进入 build 目录进行外部编译：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">cmake</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ..</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>构建完成后，你会发现生成的目标文件 hello 位于 build/bin 目录中。</p><p><strong>语法解释：</strong></p><p><strong>ADD_SUBDIRECTORY 指令</strong></p><p><code>ADD_SUBDIRECTORY(source_dir [binary_dir] [EXCLUDE_FROM_ALL])</code></p><p>这个指令用于向当前工程添加存放源文件的子目录，并可以指定中间二进制和目标二进制存放的位置。</p><p><code>EXCLUDE_FROM_ALL</code> 参数的含义是将这个目录从编译过程中排除，比如，工程的 example，可能就需要工程构建完成后，再进入 example 目录单独进行构建(当然，你也可以通过定义依赖来解决此类问题)。</p><p>上面的例子定义了将 <code>src 子目录</code>加入工程，并指定编译输出(包含编译中间结果)路径为 <code>bin 目录</code>。如果不进行 <code>bin 目录</code>的指定，那么编译结果(包括中间结果)都将存放在 <code>build/src 目录</code>(这个目录跟原有的 src 目录对应)，指定 <code>bin 目录</code>后，相当于在编译时将 src 重命名为 bin，所有的中间结果和目标二进制都将存放在 <code>bin 目录</code>。</p><p>这里需要提一下的是 <code>SUBDIRS 指令</code>，使用方法是：</p><p><code>SUBDIRS(dir1 dir2...)</code>，但是这个指令已经不推荐使用。它可以一次添加多个子目录，并且，即使外部编译，子目录体系仍然会被保存。</p><p>如果我们在上面的例子中将 <code>ADD_SUBDIRECTORY (src bin)</code> 修改为 <code>SUBDIRS(src)</code>，那么在 build 目录中将出现一个 src 目录，生成的目标代码 hello 将存放在 src 目录中。</p><p>更推荐使用 <code>ADD_SUBDIRECTORY</code>，因其功能更灵活、现代。</p><h2 id="_3-换个地方保存目标二进制" tabindex="-1">3.换个地方保存目标二进制 <a class="header-anchor" href="#_3-换个地方保存目标二进制" aria-label="Permalink to &quot;3.换个地方保存目标二进制&quot;">​</a></h2><p>不论是 SUBDIRS 还是 ADD_SUBDIRECTORY 指令(不论是否指定编译输出目录)，我们都可以通过 SET 指令重新定义 <code>EXECUTABLE_OUTPUT_PATH</code> 和 <code>LIBRARY_OUTPUT_PATH</code> 变量来指定最终的目标二进制的位置（指最终生成的 hello 或者最终的共享库，不包含编译生成的中间文件）。</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">SET(EXECUTABLE_OUTPUT_PATH</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> \${PROJECT_BINARY_DIR}</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">/bin</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">SET(LIBRARY_OUTPUT_PATH</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> \${PROJECT_BINARY_DIR}</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">/lib</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>含义如下：</p><ul><li><p><code>EXECUTABLE_OUTPUT_PATH</code>：设置最终生成的可执行文件（如 <code>hello</code>）输出到 <code>build/bin</code>。</p></li><li><p><code>LIBRARY_OUTPUT_PATH</code>：设置最终生成的库文件输出到 <code>build/lib</code>。</p></li></ul><p>在第一节我们提到了 <code>&lt;projectname&gt;_BINARY_DIR</code> 和 <code>PROJECT_BINARY_DIR</code> 变量，它们指的是编译发生的当前目录，如果是内部编译，就相当于 <code>PROJECT_SOURCE_DIR</code> 也就是工程代码所在目录，如果是外部编译，指的是外部编译所在目录，也就是本例中的 <code>build 目录</code>。</p><p>所以，上面两个指令分别定义了：</p><p>可执行二进制的输出路径为 <code>build/bin</code> 和库的输出路径为 <code>build/lib</code>。</p><p>本节我们没有提到共享库和静态库的构建，所以，你可以不考虑第二条指令。</p><p>问题是，我应该把这两条指令写在<code>工程的 CMakeLists.txt</code> 还是 <code>src 目录下的CMakeLists.txt</code>，把握一个简单的原则，在哪里 <code>ADD_EXECUTABLE</code> 或 <code>ADD_LIBRARY</code>，如果需要改变目标存放路径，就在哪里加入上述的定义。</p><p>在这个例子里，当然就是指 <code>src 下的 CMakeLists.txt</code> 了。</p><h2 id="_4-如何安装" tabindex="-1">4.如何安装 <a class="header-anchor" href="#_4-如何安装" aria-label="Permalink to &quot;4.如何安装&quot;">​</a></h2><h3 id="_4-1-cmake-的安装机制简明概览" tabindex="-1">4.1 CMake 的安装机制简明概览 <a class="header-anchor" href="#_4-1-cmake-的安装机制简明概览" aria-label="Permalink to &quot;4.1 CMake 的安装机制简明概览&quot;">​</a></h3><p>CMake 安装支持两种使用场景：</p><ol><li><p><strong>直接安装</strong>：通过 <code>make install</code> 将程序安装到系统目录（如 <code>/usr/bin</code>）。</p></li><li><p><strong>打包安装</strong>：通过设置 <code>DESTDIR</code> 将安装内容临时放入指定路径，用于打包。</p></li></ol><p><strong>手写 Makefile 示例</strong></p><div class="language-MakeFile vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">MakeFile</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">DESTDIR=</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">install</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    mkdir -p </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">$(</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">DESTDIR</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">/usr/bin</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    install -m 755 hello </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">$(</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">DESTDIR</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">/usr/bin</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>说明：</p><ul><li><p><code>make install</code>：安装到 <code>/usr/bin</code></p></li><li><p><code>make install DESTDIR=/tmp/test</code>：安装到 <code>/tmp/test/usr/bin</code>，用于打包</p></li></ul><p>若再引入 <code>PREFIX</code>，则：</p><div class="language-MakeFile vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">MakeFile</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">DESTDIR=</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">PREFIX=/usr</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">install</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    mkdir -p </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">$(</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">DESTDIR</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">$(</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">PREFIX</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">/bin</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    install -m 755 hello </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">$(</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">DESTDIR</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">$(</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">PREFIX</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">/bin</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>这正是 autotools 类工具中 <code>./configure --prefix=/usr</code> 的作用。</p><p><strong>CMake 安装对应方式</strong></p><p>CMake 中提供了两大核心机制：</p><ol><li><p><code>CMAKE_INSTALL_PREFIX</code>：相当于 <code>PREFIX</code></p></li><li><p><code>DESTDIR</code>：临时根目录前缀，常用于打包</p></li></ol><p>配置命令：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">cmake</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -DCMAKE_INSTALL_PREFIX=/usr</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ..</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>执行安装：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> install</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                 # 安装到 /usr/bin 等</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> install</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> DESTDIR=/tmp/p</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> # 实际安装到 /tmp/p/usr/bin</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p><code>CMAKE_INSTALL_PREFIX</code> 变量 等价于传统 <code>./configure --prefix=...</code></p><h3 id="_4-2-install-指令详解" tabindex="-1">4.2 INSTALL 指令详解 <a class="header-anchor" href="#_4-2-install-指令详解" aria-label="Permalink to &quot;4.2 INSTALL 指令详解&quot;">​</a></h3><ol><li><p><strong>安装目标文件（可执行程序、库）</strong></p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">INSTALL</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(TARGETS target1 [target2 ...]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        [RUNTIME DESTINATION &lt;dir&gt;]    </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 可执行文件</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        [LIBRARY DESTINATION &lt;dir&gt;]    </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 动态库</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        [ARCHIVE DESTINATION &lt;dir&gt;])   </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 静态库</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><ul><li><p><code>TARGETS</code>：由 <code>add_executable</code> 或 <code>add_library</code> 生成的目标</p></li><li><p><code>DESTINATION</code>：为相对路径时，会附加在 <code>CMAKE_INSTALL_PREFIX</code> 后</p></li><li><p>自动处理 Debug/Release 等构建模式的库路径</p></li></ul><blockquote><p>不用关心目标生成路径，直接指定目标名即可</p></blockquote><p>✅ <strong>示例：</strong></p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">INSTALL</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(TARGETS myrun mylib mystaticlib</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        RUNTIME DESTINATION bin</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        LIBRARY DESTINATION lib</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        ARCHIVE DESTINATION libstatic)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>安装后路径为：</p><ul><li><p>可执行文件：<code>\${CMAKE_INSTALL_PREFIX}/bin/myrun</code></p></li><li><p>动态库：<code>\${CMAKE_INSTALL_PREFIX}/lib/mylib.so</code></p></li><li><p>静态库：<code>\${CMAKE_INSTALL_PREFIX}/libstatic/libmystaticlib.a</code></p></li></ul><hr></li><li><p><strong>安装普通文件（如配置、文档等）</strong></p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">INSTALL</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(FILES file1 file2 ...</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        DESTINATION &lt;dir&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        [PERMISSIONS ...]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        [RENAME &lt;newname&gt;]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        [OPTIONAL])</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><ul><li><p>默认权限为 <code>644</code>（OWNER_WRITE, OWNER_READ, GROUP_READ, WORLD_READ）</p></li><li><p>可通过 <code>PERMISSIONS</code> 自定义权限</p></li></ul><p>✅ 示例：</p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">INSTALL</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(FILES config.ini README.md DESTINATION etc/myproj)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><hr></li><li><p><strong>安装可执行脚本（非目标）</strong></p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">INSTALL</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(PROGRAMS script1.sh ...</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        DESTINATION &lt;dir&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        [PERMISSIONS ...]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        [RENAME &lt;newname&gt;]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        [OPTIONAL])</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><ul><li><p>用于安装非构建生成的脚本、可执行程序</p></li><li><p>默认权限为 <code>755</code>（OWNER_EXECUTE 等）</p></li></ul><p>✅ 示例：</p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">INSTALL</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(PROGRAMS run.sh DESTINATION bin)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div></li><li><p><strong>安装目录及其内容</strong></p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">INSTALL</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(PROGRAMS script1.sh ...</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        DESTINATION &lt;dir&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        [PERMISSIONS ...]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        [RENAME &lt;newname&gt;]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        [OPTIONAL])</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><ul><li><p><code>DIRECTORY</code> 后面的路径 <strong>若无斜杠</strong>，目录本身会被复制</p></li><li><p>加 <code>/</code> 则只复制目录内容</p></li><li><p><code>PATTERN</code>：匹配过滤文件/目录，可配合 <code>EXCLUDE</code> 或 <code>PERMISSIONS</code> 使用</p></li></ul><p>✅ 示例：</p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">INSTALL</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(DIRECTORY icons scripts/ DESTINATION share/myproj</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        PATTERN </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;CVS&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> EXCLUDE</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        PATTERN </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;scripts/*&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        PERMISSIONS OWNER_EXECUTE OWNER_WRITE OWNER_READ GROUP_EXECUTE GROUP_READ)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>此例说明：</p><ul><li><p><code>icons</code> 目录整体复制到 <code>share/myproj/icons</code></p></li><li><p><code>scripts/</code> 中的内容复制到 <code>share/myproj</code>（目录本身不复制）</p></li><li><p>忽略名为 <code>CVS</code> 的文件/目录</p></li><li><p>对 <code>scripts/*</code> 设置指定权限</p></li></ul><hr></li><li><p><strong>安装时执行脚本或代码</strong></p></li></ol><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">INSTALL</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(SCRIPT some_script.cmake)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">INSTALL</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(CODE </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;MESSAGE(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">\\&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">Installing something...</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">\\&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">)&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><ul><li><p><code>SCRIPT</code> 执行外部 <code>.cmake</code> 文件</p></li><li><p><code>CODE</code> 直接执行 CMake 命令（字符串）</p></li></ul><h2 id="_5-修改helloworld支持安装" tabindex="-1">5.修改Helloworld支持安装 <a class="header-anchor" href="#_5-修改helloworld支持安装" aria-label="Permalink to &quot;5.修改Helloworld支持安装&quot;">​</a></h2><p>在本节开头我们定义了本节的任务如下：</p><ol><li><p>为工程添加一个<code>子目录 src</code>，用来放置工程源代码</p></li><li><p>添加一个<code>子目录 doc</code>，用来放置这个工程的文档 <code>hello.txt</code></p></li><li><p>在工程目录添加文本文件 <code>COPYRIGHT</code>，<code>README</code></p></li><li><p>在工程目录添加一个 <code>runhello.sh</code> 脚本，用来调用 hello 二进制</p></li><li><p>将构建后的目标文件放入构建目录的 <code>bin 子目录</code></p></li><li><p>最终安装这些文件：将 <code>hello</code> 二进制与 <code>runhello.sh</code> 安装至<code>/&lt;prefix&gt;/bin</code>，将<code>doc 目录</code>的内容以及 <code>COPYRIGHT/README</code> 安装到<code>/&lt;prefix&gt;/share/doc/cmake/t2</code></p></li></ol><p>首先我们先补上未添加的文件：</p><h3 id="_5-1-添加-doc-目录及文件" tabindex="-1">5.1 添加 <code>doc 目录</code>及文件 <a class="header-anchor" href="#_5-1-添加-doc-目录及文件" aria-label="Permalink to &quot;5.1 添加 \`doc 目录\`及文件&quot;">​</a></h3><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">cd</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /backup/cmake/t2</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">mkdir</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> doc</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">vi</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> doc/hello.txt</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p><code>hello.txt</code>随便填写一些内容并保存</p><h3 id="_5-2-在工程目录添加-runhello-sh-脚本" tabindex="-1">5.2 在工程目录添加 <code>runhello.sh</code> 脚本 <a class="header-anchor" href="#_5-2-在工程目录添加-runhello-sh-脚本" aria-label="Permalink to &quot;5.2 在工程目录添加 \`runhello.sh\` 脚本&quot;">​</a></h3><p>内容为：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">./hello</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><h3 id="_5-3-添加工程目录中的-copyright-和-readme" tabindex="-1">5.3 添加工程目录中的 <code>COPYRIGHT</code> 和 <code>README</code> <a class="header-anchor" href="#_5-3-添加工程目录中的-copyright-和-readme" aria-label="Permalink to &quot;5.3 添加工程目录中的 \`COPYRIGHT\` 和 \`README\`&quot;">​</a></h3><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">touch</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> COPYRIGHT</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">touch</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> README</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><h3 id="_5-4-下面改写各目录的-cmakelists-txt" tabindex="-1">5.4 下面改写各目录的 CMakeLists.txt <a class="header-anchor" href="#_5-4-下面改写各目录的-cmakelists-txt" aria-label="Permalink to &quot;5.4 下面改写各目录的 CMakeLists.txt&quot;">​</a></h3><ol><li><p>安装 COPYRIGHT/README，直接修改主工程文件 CMakelists.txt，加入以下指令：</p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">INSTALL</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(FILES COPYRIGHT README DESTINATION share/</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">doc</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">/cmake/t2)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div></li><li><p>安装 runhello.sh，直接修改主工程文件 CMakeLists.txt，加入如下指令：</p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">INSTALL</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(PROGRAMS runhello.sh DESTINATION bin)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div></li><li><p>安装 doc 中的 hello.txt，这里有两种方式：</p><p>一是通过在 doc 目录建立 CMakeLists.txt 并将 doc 目录通<code>ADD_SUBDIRECTORY</code> 加入工程来完成。</p><p>另一种方法是直接在工程目录通过 <code>INSTALL(DIRECTORY )</code>来完成，前者比较简单，各位可以根据兴趣自己完成，我们来尝试后者，顺便演示以下 DIRECTORY 的安装。</p><p>因为 hello.txt 要安装到 <code>/prefix/share/doc/cmake/t2</code>，所以我们不能直接安装整个 doc 目录，这里采用的方式是安装 doc 目录中的内容，也就是使用 <code>doc/</code>。</p><p>在工程文件中添加：</p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">INSTALL</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(DIRECTORY </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">doc</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">/ DESTINATION share/</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">doc</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">/cmake/t2)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div></li><li><p>在 <code>src</code>目录的 <code>CMakeLists.txt</code>文件中添加如下内容，以安装 hello 到 <code>/&lt;prefix&gt;/bin</code>中：</p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">INSTALL</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(TARGETS hello RUNTIME DESTINATION bin)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div></li></ol><h2 id="_6-尝试我们修改的结果" tabindex="-1">6.尝试我们修改的结果 <a class="header-anchor" href="#_6-尝试我们修改的结果" aria-label="Permalink to &quot;6.尝试我们修改的结果&quot;">​</a></h2><p>现在进入 build 目录进行外部编译，注意使用 <code>CMAKE_INSTALL_PREFIX</code> 参数，这里我们将它安装到了<code>/tmp/t2</code>目录：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">cmake</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -DCMAKE_INSTALL_PREFIX=/tmp/t2/usr</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ..</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>然后运行：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> install</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>让我们进入/tmp/t2 目录看一下安装结果：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">./usr</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">./usr/share</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">./usr/share/doc</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">./usr/share/doc/cmake</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">./usr/share/doc/cmake/t2</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">./usr/share/doc/cmake/t2/hello.txt</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">./usr/share/doc/cmake/t2/README</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">./usr/share/doc/cmake/t2/COPYRIGHT</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">./usr/bin</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">./usr/bin/hello</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">./usr/bin/runhello.sh</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><p>如果你要直接安装到系统，可以使用如下指令：</p><p><code>cmake -DCMAKE_INSTALL_PREFIX=/usr ..</code></p><hr><h2 id="_7-一个疑问" tabindex="-1">7.一个疑问 <a class="header-anchor" href="#_7-一个疑问" aria-label="Permalink to &quot;7.一个疑问&quot;">​</a></h2><p>如果我没有定义 <code>CMAKE_INSTALL_PREFIX</code> 会安装到什么地方？</p><p>你可以尝试以下，<code>cmake ..;make;make install</code>，你会发现 <code>CMAKE_INSTALL_PREFIX</code> 的默认定义是 <code>/usr/local</code>。</p><hr><h2 id="_8-小结" tabindex="-1">8.小结 <a class="header-anchor" href="#_8-小结" aria-label="Permalink to &quot;8.小结&quot;">​</a></h2><p>本小节主要描述了如何在工程中使用多目录、各种安装指令以及 CMAKE_INSTALL_PREFIX 变量(你真够牛的，这么点东西居然罗唆了这么多文字)。</p><p>在下一小节，我们将探讨如何在 cmake 中构建动态库和静态库，以及如何使用外部头文件和外部共享库，毕竟，这是程序编写中最长使用的（对了，你知道用怎样的 gcc 参数可以直接构建静态库和动态库吗？）</p><h1 id="五、静态库和动态库的构建" tabindex="-1">五、静态库和动态库的构建 <a class="header-anchor" href="#五、静态库和动态库的构建" aria-label="Permalink to &quot;五、静态库和动态库的构建&quot;">​</a></h1><p>读者云，太能罗唆了，一个 Hello World 就折腾了两个大节。OK，从本节开始，我们不再折腾 Hello World 了，我们来折腾 Hello World 的共享库。</p><p>本节的任务：</p><ol><li><p>建立一个静态库和动态库，提供 <code>HelloFunc 函数</code>供其他程序编程使用，HelloFunc 向终端输出 Hello World 字符串。</p></li><li><p>安装头文件与共享库。</p></li></ol><h2 id="_1-准备工作-2" tabindex="-1">1.准备工作 <a class="header-anchor" href="#_1-准备工作-2" aria-label="Permalink to &quot;1.准备工作&quot;">​</a></h2><p>在 <code>/backup/cmake</code> 目录建立 <code>t3 目录</code>，用于存放本节涉及到的工程。</p><h2 id="_2-建立共享库" tabindex="-1">2.建立共享库 <a class="header-anchor" href="#_2-建立共享库" aria-label="Permalink to &quot;2.建立共享库&quot;">​</a></h2><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">cd</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /backup/cmake/t3</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">mkdir</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> lib</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>在 t3 目录下建立 <code>CMakeLists.txt</code>，内容如下：</p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">PROJECT</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(HELLOLIB)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">ADD_SUBDIRECTORY</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(lib)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>在 lib 目录下建立两个源文件 <code>hello.c</code> 与 <code>hello.h</code></p><p><code>hello.c</code> 内容如下：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">#include &quot;hello.h&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">void</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> HelloFunc</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() </span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    printf</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">&quot;Hello World\\n&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p><code>hello.h</code> 内容如下：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">#ifndef HELLO_H</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">#define HELLO_H</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">#include &lt;stdio.h&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">void</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> HelloFunc</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">();</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">#endif</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>在 lib 目录下建立 CMakeLists.txt，内容如下：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">SET(LIBHELLO_SRC</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> hello.c</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ADD_LIBRARY(hello</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> SHARED</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> \${LIBHELLO_SRC})</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><h2 id="_3-编译共享库" tabindex="-1">3.编译共享库 <a class="header-anchor" href="#_3-编译共享库" aria-label="Permalink to &quot;3.编译共享库&quot;">​</a></h2><p>仍然采用 out-of-source 编译的方式，按照习惯，我们建立一个 build 目录，在 build 目录中：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">cmake</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ..</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>这时，你就可以在 lib 目录得到一个 <code>libhello.so</code>，这就是我们期望的共享库。</p><blockquote><p>在Windows的Cygwin环境下，该共享库为 <code>cyghello.dll</code>。</p></blockquote><p>如果你要指定 <code>libhello.so</code> 生成的位置，可以通过在主工程文件 CMakeLists.txt 中修改 <code>ADD_SUBDIRECTORY(lib)</code> 指令来指定一个编译输出位置或者在 <code>lib/CMakeLists.txt</code> 中添加 <code>SET(LIBRARY_OUTPUT_PATH &lt;路径&gt;)</code> 来指定一个新的位置。</p><p>这两者的区别我们上一节已经提到了，所以，这里不再赘述，下面，我们解释一下一个新的指令 <code>ADD_LIBRARY</code> ：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ADD_LIBRARY(libname</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> [SHARED</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">|</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">STATIC</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">|</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">MODULE]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> [EXCLUDE_FROM_ALL]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> source1</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> source2</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ...</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> sourceN</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>你不需要写全 <code>libhello.so</code>，只需要填写 <code>hello</code> 即可，cmake 系统会自动为你生成 <code>libhello.X</code>。</p><p>类型有三种：</p><ol><li><p>SHARED，动态库</p></li><li><p>STATIC，静态库</p></li><li><p>MODULE，在使用 dyld 的系统有效，如果不支持 dyld，则被当作 SHARED 对待。</p></li></ol><p><code>EXCLUDE_FROM_ALL</code> 参数的意思是这个库不会被默认构建，除非有其他的组件依赖或者手工构建。</p><h2 id="_4-添加静态库" tabindex="-1">4. 添加静态库 <a class="header-anchor" href="#_4-添加静态库" aria-label="Permalink to &quot;4. 添加静态库&quot;">​</a></h2><p>同样使用上面的指令，我们在支持动态库的基础上再为工程添加一个静态库，按照一般的习惯，静态库名字跟动态库名字应该是一致的，只不过后缀是.a 罢了。</p><p>下面我们用这个指令再来添加静态库：</p><p><code>ADD_LIBRARY(hello STATIC \${LIBHELLO_SRC})</code></p><p>然后再在 build 目录进行外部编译，我们会发现，静态库根本没有被构建，仍然只生成了一个动态库。因为 hello 作为一个 target 是不能重名的，所以，静态库构建指令无效。</p><p>如果我们把上面的 <code>hello</code> 修改为 <code>hello_static</code>：</p><p><code>ADD_LIBRARY(hello_static STATIC \${LIBHELLO_SRC})</code></p><p>就可以构建一个 <code>libhello_static.a</code> 的静态库了。</p><p>这种结果显示不是我们想要的，我们需要的是名字相同的静态库和动态库，因为 target 名称是唯一的，所以，我们肯定不能通过 ADD_LIBRARY 指令来实现了。这时候我们需要用到另外一个指令：</p><p><code>SET_TARGET_PROPERTIES</code>，其基本语法是：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">SET_TARGET_PROPERTIES(target1</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> target2</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ...</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> PROPERTIES</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> prop1</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> value1</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> prop2</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> value2</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ...</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>这条指令可以用来设置输出的名称，对于动态库，还可以用来指定动态库版本和 API 版本。</p><p>在本例中，我们需要作的是向 <code>lib/CMakeLists.txt</code> 中添加一条：</p><p><code>SET_TARGET_PROPERTIES(hello_static PROPERTIES OUTPUT_NAME &quot;hello&quot;)</code></p><p>这样，我们就可以同时得到 <code>libhello.so/libhello.a</code> 两个库了。</p><p>与它对应的指令是：</p><p><code>GET_TARGET_PROPERTY(VAR target property)</code></p><p>具体用法如下例，我们向 <code>lib/CMakeListst.txt</code> 中添加：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">GET_TARGET_PROPERTY(OUTPUT_VALUE</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> hello_static</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> OUTPUT_NAME</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">MESSAGE(STATUS</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> “This</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> is</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> the</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> hello_static</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> OUTPUT_NAME:”</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> \${OUTPUT_VALUE})</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>如果没有这个属性定义，则返回 NOTFOUND。</p><p>让我们来检查一下最终的构建结果，我们发现，<code>libhello.a</code> 已经构建完成，位于 <code>build/lib</code> 目录中，但是 <code>libhello.so</code> 却消失了。这个问题的原因是：cmake 在构建一个新的 target 时，会尝试清理掉其他使用这个名字的库，因此，在构建 <code>libhello.a</code> 时，就会清理掉 <code>libhello.so</code>。</p><p>为了回避这个问题，比如再次使用 <code>SET_TARGET_PROPERTIES</code> 定义 <code>CLEAN_DIRECT_OUTPUT</code> 属性。</p><p>向 <code>lib/CMakeLists.txt</code> 中添加：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">SET_TARGET_PROPERTIES(hello</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> PROPERTIES</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> CLEAN_DIRECT_OUTPUT</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">SET_TARGET_PROPERTIES(hello_static</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> PROPERTIES</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> CLEAN_DIRECT_OUTPUT</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>这时候，我们再次进行构建，会发现 <code>build/lib</code> 目录中同时生成了 <code>libhello.so</code> 和 <code>libhello.a</code>。</p><h2 id="_5-动态库版本号" tabindex="-1">5. 动态库版本号 <a class="header-anchor" href="#_5-动态库版本号" aria-label="Permalink to &quot;5. 动态库版本号&quot;">​</a></h2><p>按照规则，动态库是应该包含一个版本号的，我们可以看一下系统的动态库，一般情况是：</p><p>libhello.so.1.2</p><p>libhello.so -&gt;libhello.so.1</p><p>libhello.so.1-&gt;libhello.so.1.2</p><p>为了实现动态库版本号，我们仍然需要使用 <code>SET_TARGET_PROPERTIES</code> 指令。</p><p>具体使用方法如下：</p><p><code>SET_TARGET_PROPERTIES(hello PROPERTIES VERSION 1.2 SOVERSION 1)</code></p><p>VERSION 指代动态库版本，SOVERSION 指代 API 版本。</p><p>将上述指令加入 <code>lib/CMakeLists.txt</code> 中，重新构建看看结果。</p><p>在 build/lib 目录会生成：</p><p>libhello.so.1.2</p><p>libhello.so.1-&gt;libhello.so.1.2</p><p>libhello.so -&gt;libhello.so.1</p><h2 id="_6-安装共享库和头文件" tabindex="-1">6. 安装共享库和头文件 <a class="header-anchor" href="#_6-安装共享库和头文件" aria-label="Permalink to &quot;6. 安装共享库和头文件&quot;">​</a></h2><p>以上面的例子，我们需要将 <code>libhello.a</code>, <code>libhello.so.x</code> 以及 <code>hello.h</code> 安装到系统目录，才能真正让其他人开发使用，在本例中我们将 hello 的共享库安装到 <code>/lib</code> 目录，将 <code>hello.h</code> 安装到 <code>/include/hello</code> 目录。</p><p>利用上一节了解到的 <code>INSTALL 指令</code>，我们向 <code>lib/CMakeLists.txt</code> 中添加如下指令：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">INSTALL(TARGETS</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> hello</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> hello_static</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">    LIBRARY</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> DESTINATION</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> lib</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">    ARCHIVE</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> DESTINATION</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> lib</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">INSTALL(FILES</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> hello.h</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> DESTINATION</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> include/hello</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>注意，静态库要使用 ARCHIVE 关键字。</p><p>通过：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">cmake</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -DCMAKE_INSTALL_PREFIX=/usr</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ..</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> install</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>我们就可以将头文件和共享库安装到系统目录 <code>/usr/lib</code> 和 <code>/usr/include/hello</code> 中了。</p><p>在Mac中，请安装到 <code>/usr/local/lib目录下</code>。</p><h2 id="_7-小结" tabindex="-1">7.小结 <a class="header-anchor" href="#_7-小结" aria-label="Permalink to &quot;7.小结&quot;">​</a></h2><p>本小节，我们谈到了：</p><ul><li><p>如何通过 <code>ADD_LIBRARY</code> 指令构建动态库和静态库。</p></li><li><p>如何通过 <code>SET_TARGET_PROPERTIES</code> 同时构建同名的动态库和静态库。</p></li><li><p>如何通过 <code>SET_TARGET_PROPERTIES</code> 控制动态库版本。</p></li><li><p>最终使用上一节谈到的 <code>INSTALL 指令</code>来安装头文件和动态、静态库。</p></li></ul><p>在下一节，我们需要编写另一个高级一点的 Hello World 来演示怎么使用我们已经构建的构建的共享库 libhello 和外部头文件。</p><h1 id="六、如何使用外部共享库和" tabindex="-1">六、如何使用外部共享库和 <a class="header-anchor" href="#六、如何使用外部共享库和" aria-label="Permalink to &quot;六、如何使用外部共享库和&quot;">​</a></h1><p>抱歉，本节仍然继续折腾 Hello World。</p><p>上一节我们已经完成了 libhello 动态库的构建以及安装，本节我们的任务很简单：</p><ul><li>编写一个程序使用我们上一节构建的共享库。</li></ul><h2 id="_1-准备工作-3" tabindex="-1">1. 准备工作 <a class="header-anchor" href="#_1-准备工作-3" aria-label="Permalink to &quot;1. 准备工作&quot;">​</a></h2><p>请在 <code>/backup/cmake</code> 目录建立 <code>t4 目录</code>，本节所有资源将存储在 t4 目录。</p><h2 id="_2-重复以前的步骤-建立src目录-编写源文件main-c-内容如下" tabindex="-1">2.重复以前的步骤，建立src目录，编写源文件main.c，内容如下： <a class="header-anchor" href="#_2-重复以前的步骤-建立src目录-编写源文件main-c-内容如下" aria-label="Permalink to &quot;2.重复以前的步骤，建立src目录，编写源文件main.c，内容如下：&quot;">​</a></h2><div class="language-C vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">C</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">#include</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &lt;hello.h&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">int</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> main</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">void</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">    HelloFunc</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">();</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    </span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    return</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>编写工程主文件 <code>CMakeLists.txt</code>：</p><div class="language-C vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">C</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">PROJECT</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(HELLO)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ADD_SUBDIRECTORY</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(src)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>编写 <code>src/CMakeLists.txt</code>：</p><div class="language-C vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">C</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ADD_EXECUTABLE</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(main main.c)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><hr><h2 id="_3-外部构建" tabindex="-1">3.外部构建 <a class="header-anchor" href="#_3-外部构建" aria-label="Permalink to &quot;3.外部构建&quot;">​</a></h2><p>按照习惯，仍然建立 <code>build 目录</code>，使用 <code>cmake ..</code> 方式构建。</p><p>过程：</p><div class="language-C vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">C</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">cmake ..</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">make</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>构建失败，如果需要查看细节，可以使用第一节提到的方法：<code>make VERBOSE=1</code> 来构建。</p><p>错误输出为是：</p><div class="language-C vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">C</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">backup</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">cmake</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">t4</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">src</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">main.c:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">19</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: error: hello.h: 没有那个文件或目录</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><h2 id="_4-引入头文件搜索路径" tabindex="-1">4.引入头文件搜索路径 <a class="header-anchor" href="#_4-引入头文件搜索路径" aria-label="Permalink to &quot;4.引入头文件搜索路径&quot;">​</a></h2><p>有时候我们在编写 C/C++ 工程时，使用的库（比如 <code>hello</code> 库）头文件并不位于系统默认的标准路径（如 <code>/usr/include</code>），而是放在了某个自定义路径下，比如</p><p><code>/usr/include/hello/hello.h</code></p><p>此时，编译器就无法直接找到该头文件。</p><p><strong>❌ 错误示范：</strong></p><p>很多新手第一反应是直接这样写：</p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">#include &lt;hello.h&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>然后编译器报错：<code>fatal error: hello.h: No such file or directory</code>。如果你不告诉编译器这个头文件在哪里，它当然找不到。</p><p><strong>✅ 正确做法：使用 <code>INCLUDE_DIRECTORIES</code> 告诉 CMake 头文件位置</strong></p><p>我们可以在 <code>CMakeLists.txt</code> 中使用 <code>INCLUDE_DIRECTORIES</code> 命令，显式添加头文件搜索路径。例如，在 <code>src/CMakeLists.txt</code> 中添加：</p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">include_directories</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(/usr/include/hello)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>或者更现代的推荐方式是使用 <strong><code>target_include_directories</code></strong>：</p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">add_executable</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(demo main.c)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">target_include_directories</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(demo </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">PRIVATE</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> /usr/include/hello)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>这告诉编译器去 <code>/usr/include/hello</code> 目录下找头文件。之后，我们就可以在代码中正常使用：</p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">#include &lt;hello.h&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>现在，头文件找到了，但问题还没完！</p><p>如果你尝试编译：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">gcc</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> main.c</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -o</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> demo</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>可能会出现如下链接错误：</p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">main.c:(.text+0x12): undefined reference to \`HelloFunc&#39;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>这表明虽然头文件被找到了，但编译器在链接阶段找不到 <code>HelloFunc</code> 函数的定义——它应该是在 <code>libhello.so</code> 或 <code>libhello.a</code> 中。</p><h2 id="_5-为target添加共享库" tabindex="-1">5.为target添加共享库 <a class="header-anchor" href="#_5-为target添加共享库" aria-label="Permalink to &quot;5.为target添加共享库&quot;">​</a></h2><p>我们这一步的目标是让可执行程序 <code>main</code> 能用上我们之前写的共享库 <code>libhello</code>。为此，需要用到两个新的 CMake 指令：<code>LINK_DIRECTORIES</code> 和 <code>TARGET_LINK_LIBRARIES</code>。</p><h3 id="_5-1-link-directories-——-指定库文件在哪" tabindex="-1">5.1 <code>LINK_DIRECTORIES</code> —— 指定库文件在哪 <a class="header-anchor" href="#_5-1-link-directories-——-指定库文件在哪" aria-label="Permalink to &quot;5.1 \`LINK_DIRECTORIES\` —— 指定库文件在哪&quot;">​</a></h3><p><strong>用法：</strong></p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">LINK_DIRECTORIES</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(路径1 路径2 ...)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>这个指令的作用是告诉编译器“去这些路径下找库文件”。 通常只有在库文件不在系统默认路径（比如 <code>/usr/lib</code>）下时才需要用它。</p><blockquote><p>本例中我们用不到它，可以先不用管。</p></blockquote><hr><h3 id="_5-2target-link-libraries-——-把库-挂-到目标程序上" tabindex="-1">5.2<code>TARGET_LINK_LIBRARIES</code> —— 把库“挂”到目标程序上 <a class="header-anchor" href="#_5-2target-link-libraries-——-把库-挂-到目标程序上" aria-label="Permalink to &quot;5.2\`TARGET_LINK_LIBRARIES\` —— 把库“挂”到目标程序上&quot;">​</a></h3><p><strong>用法：</strong></p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">TARGET_LINK_LIBRARIES</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(目标程序 库名1 [库名2 ...])</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>这个指令就是把某个库链接到你要编译的程序或库上。</p><p>比如我们要把 <code>libhello</code> 加到 <code>main</code> 上，只需要在 <code>src/CMakeLists.txt</code> 里加上一行：</p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">TARGET_LINK_LIBRARIES</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(main hello)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>也可以写得更明确一些：</p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">TARGET_LINK_LIBRARIES</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(main libhello.so)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>这里的 <code>hello</code> 对应的是我们之前生成的共享库 <code>libhello.so</code>（系统在链接时会自动补上前缀 <code>lib</code> 和后缀 <code>.so</code>）。</p><hr><h3 id="_5-3-编译并运行" tabindex="-1">5.3 编译并运行 <a class="header-anchor" href="#_5-3-编译并运行" aria-label="Permalink to &quot;5.3 编译并运行&quot;">​</a></h3><p>回到 <code>build</code> 目录，重新构建项目：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">cmake</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ..</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>如果一切正常，你会在 <code>build/src</code> 目录下看到一个可执行文件 <code>main</code>。运行它：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">./src/main</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>输出应该是：</p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">Hello World</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>说明它已经成功调用了 <code>libhello</code> 提供的函数。</p><hr><h3 id="_5-4-检查一下链接情况" tabindex="-1">5.4 检查一下链接情况 <a class="header-anchor" href="#_5-4-检查一下链接情况" aria-label="Permalink to &quot;5.4 检查一下链接情况&quot;">​</a></h3><p>以用 <code>ldd</code> 命令看看 <code>main</code> 实际链接了哪些库：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ldd</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> src/main</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>输出可能长这样：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">linux-gate.so.1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  (0xb7ee7000)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">libhello.so.1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /usr/lib/libhello.so.1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (0xb7ece000)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">libc.so.6</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /lib/libc.so.6</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (0xb7d77000)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">/lib/ld-linux.so.2</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (0xb7ee8000)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>能看到 <code>libhello.so.1</code>，说明 <code>main</code> 已经和我们写的共享库连上了。</p><hr><h3 id="_5-5-如果你想链接静态库呢" tabindex="-1">5.5 如果你想链接静态库呢？ <a class="header-anchor" href="#_5-5-如果你想链接静态库呢" aria-label="Permalink to &quot;5.5 如果你想链接静态库呢？&quot;">​</a></h3><p>也很简单，只要把链接指令改成：</p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">TARGET_LINK_LIBRARIES</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(main libhello.a)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>然后重新构建项目。</p><p>再次运行 <code>ldd src/main</code>，这次你会发现：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">linux-gate.so.1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  (0xb7fa8000)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">libc.so.6</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /lib/libc.so.6</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (0xb7e3a000)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">/lib/ld-linux.so.2</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (0xb7fa9000)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p><code>libhello.so</code> 不见了！说明程序这次是把 <code>libhello.a</code> 静态编译进去了，不再依赖外部共享库。</p><h3 id="总结" tabindex="-1">总结： <a class="header-anchor" href="#总结" aria-label="Permalink to &quot;总结：&quot;">​</a></h3><ul><li><p><code>TARGET_LINK_LIBRARIES(main hello)</code> 就是把 <code>libhello</code> 挂到 <code>main</code> 上。</p></li><li><p>默认链接的是动态库（<code>.so</code>），想用静态库（<code>.a</code>）就写上全名。</p></li><li><p>用 <code>ldd</code> 可以验证程序到底链接了哪些库。</p></li></ul><p>通过这些步骤，我们就成功地让 <code>main</code> 使用了 <code>libhello</code> 提供的功能啦！</p><hr><h2 id="_6-特殊的环境变量-cmake-include-path-和-cmake-library-path" tabindex="-1">6.特殊的环境变量：<code>CMAKE_INCLUDE_PATH</code> 和 <code>CMAKE_LIBRARY_PATH</code> <a class="header-anchor" href="#_6-特殊的环境变量-cmake-include-path-和-cmake-library-path" aria-label="Permalink to &quot;6.特殊的环境变量：\`CMAKE_INCLUDE_PATH\` 和 \`CMAKE_LIBRARY_PATH\`&quot;">​</a></h2><p>在使用 CMake 时，如果你的头文件或库文件<strong>不在标准路径</strong>（如 <code>/usr/include</code> 或 <code>/usr/lib</code>）下，就可能导致找不到文件、编译失败的问题。为了应对这种情况，CMake 提供了两个<strong>环境变量</strong>来扩展搜索路径：</p><ul><li><p><code>CMAKE_INCLUDE_PATH</code>：用于额外查找头文件的路径</p></li><li><p><code>CMAKE_LIBRARY_PATH</code>：用于额外查找库文件的路径</p></li></ul><blockquote><p>注意：它们是<strong>环境变量</strong>，不是 CMake 脚本里的变量。</p></blockquote><h3 id="_6-1-如何设置这些变量" tabindex="-1">6.1 如何设置这些变量？ <a class="header-anchor" href="#_6-1-如何设置这些变量" aria-label="Permalink to &quot;6.1 如何设置这些变量？&quot;">​</a></h3><p>在使用 <code>cmake ..</code> 前，你可以通过以下方式在终端中设置：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> CMAKE_INCLUDE_PATH</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">/usr/include/hello</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> CMAKE_LIBRARY_PATH</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">/usr/lib/mylibs</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">cmake</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ..</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>或者用一行命令快速指定：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">CMAKE_INCLUDE_PATH</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">/usr/include/hello</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> cmake</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ..</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>设置后，CMake 就会在你指定的路径中查找头文件或库。</p><hr><h3 id="_6-2这些变量的作用场景" tabindex="-1">6.2这些变量的作用场景 <a class="header-anchor" href="#_6-2这些变量的作用场景" aria-label="Permalink to &quot;6.2这些变量的作用场景&quot;">​</a></h3><p>这两个变量的作用是配合 CMake 的 <code>FIND_</code> 系列指令使用，比如 <code>FIND_PATH()</code> 查找头文件、<code>FIND_LIBRARY()</code> 查找库文件。</p><p>举个例子，我们有一个头文件 <code>hello.h</code>，它存放在 <code>/usr/include/hello/</code> 中。常规做法是写死路径：</p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">INCLUDE_DIRECTORIES</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(/usr/include/hello)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>但更通用、更灵活的写法是这样：</p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">FIND_PATH</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(myHeader hello.h)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">IF</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(myHeader)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    INCLUDE_DIRECTORIES</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">\${myHeader}</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">ENDIF</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">()</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>这样写之后，如果你设置了：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> CMAKE_INCLUDE_PATH</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">/usr/include/hello</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>CMake 会自动在指定路径中查找 <code>hello.h</code>，不需要你在 CMake 文件中写死具体位置。</p><p>同样的方式也适用于库文件查找，比如：</p><div class="language-CMake vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">FIND_LIBRARY</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(myLib hello)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>搭配：</p><div class="language-Shell vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">Shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> CMAKE_LIBRARY_PATH</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">/usr/lib/mylibs</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>CMake 就可以自动找到 <code>libhello.a</code> 或 <code>libhello.so</code>。</p><hr><h3 id="_6-3需要特别注意的点" tabindex="-1">6.3需要特别注意的点 <a class="header-anchor" href="#_6-3需要特别注意的点" aria-label="Permalink to &quot;6.3需要特别注意的点&quot;">​</a></h3><ul><li><p><strong>这两个环境变量不会直接影响编译器参数</strong>，比如不会自动加上 <code>-I</code> 或 <code>-L</code>。</p></li><li><p><strong>只有在使用 <code>FIND_PATH()</code> 或 <code>FIND_LIBRARY()</code> 这样的 CMake 查找指令时才会生效</strong>。</p></li><li><p>因为 CMake 的很多模块都是通过 <code>FIND_</code> 指令实现的，所以这两个变量对整个 CMake 系统的可移植性非常重要。</p></li></ul><h3 id="小结" tabindex="-1">小结 <a class="header-anchor" href="#小结" aria-label="Permalink to &quot;小结&quot;">​</a></h3><table tabindex="0"><thead><tr><th>环境变量</th><th>作用</th><th>使用方式</th></tr></thead><tbody><tr><td><code>CMAKE_INCLUDE_PATH</code></td><td>查找头文件的额外路径</td><td>配合 <code>FIND_PATH()</code> 使用</td></tr><tr><td><code>CMAKE_LIBRARY_PATH</code></td><td>查找库文件的额外路径</td><td>配合 <code>FIND_LIBRARY()</code> 使用</td></tr></tbody></table><p>通过使用这两个环境变量，你可以让项目在不同的环境中更加通用，不再依赖于硬编码的路径，同时也能更好地支持跨平台开发。</p><blockquote><p><a href="https://flowus.cn/share/60438529-b7ad-457e-9ef4-3dc711a685da?code=E73CZF" target="_blank" rel="noreferrer">原文</a></p></blockquote></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("knowledge/imported/qd/Project_Tutorial/cmake.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const cmake = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  cmake as default
};
